import React from 'react';

export default class SecuredInputField extends React.Component {
    render() {
        return <input id="password-field" type="password" placeholder="Password"></input>
    }
}